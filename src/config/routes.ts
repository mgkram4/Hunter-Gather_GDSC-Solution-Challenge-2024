export enum ROUTES {
  SIGNIN = "/signin",
  SIGNUP = "/signup",
  HOME = "/",
  PROFILE = "/profile",
  BOOKMARKS = "/",
  SHOPPINGLIST = "/",
  HASHTAG = "/",
  POST = "/",
  CHEF_ASSISTANT = "/chef-assistant/chat",
}

export enum API_ROUTES {
  AUTH_CALLBACK = "/auth/callback",
}
