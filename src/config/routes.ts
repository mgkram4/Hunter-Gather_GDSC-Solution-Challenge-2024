export enum ROUTES {
  RECIPE = "/recipe",
  SIGNIN = "/signin",
  SIGNUP = "/signup",
  HOME = "/",
  PROFILE = "/profile",
  BOOKMARKS = "/bookmarks",
  SHOPPINGLIST = "/",
  HASHTAG = "/",
  POST = "/",
  CHEF_ASSISTANT = "/chef-assistant/chat",
}

export enum API_ROUTES {
  AUTH_CALLBACK = "/auth/callback",
}
